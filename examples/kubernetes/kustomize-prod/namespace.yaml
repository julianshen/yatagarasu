# Namespace definition for production deployment
# Apply this first: kubectl apply -f namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: yatagarasu-prod
  labels:
    app.kubernetes.io/name: yatagarasu
    environment: production
