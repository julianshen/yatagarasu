apiVersion: v1
kind: Service
metadata:
  name: {{ include "yatagarasu.fullname" . }}
  labels:
    {{- include "yatagarasu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.metrics.enabled }}
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "yatagarasu.selectorLabels" . | nindent 4 }}
