apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yatagarasu-loadtest

resources:
  - namespace.yaml
  - minio.yaml
  - yatagarasu.yaml
  - hpa.yaml
  - pdb.yaml
  - tests.yaml

commonLabels:
  app.kubernetes.io/part-of: yatagarasu-loadtest
