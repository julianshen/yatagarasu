apiVersion: apps/v1
kind: Deployment
metadata:
  name: yatagarasu
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: yatagarasu
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
