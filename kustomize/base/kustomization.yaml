apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: yatagarasu-base

commonLabels:
  app.kubernetes.io/name: yatagarasu
  app.kubernetes.io/component: s3-proxy

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - serviceaccount.yaml

images:
  - name: ghcr.io/julianshen/yatagarasu
    newTag: latest
